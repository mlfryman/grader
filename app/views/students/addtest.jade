extends ../shared/template

block content

  h1 Adding test score #{student.name}
  form(method='post' action='/students/#{student._id}/test')
    label Score
    input(type='number', name='score')
    button Add test
